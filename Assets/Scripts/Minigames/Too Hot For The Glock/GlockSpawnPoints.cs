using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GlockSpawnPoints : MonoBehaviour
{
    public bool hasBomb;

    // Start is called before the first frame update
    void Start()
    {
        hasBomb = false;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
